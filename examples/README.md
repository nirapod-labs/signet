# examples/

Per-framework example apps (Flutter, React Native, Kotlin Multiplatform) that exercise the full generate, sign, read-tier, and attest flow on real hardware. Each is also a CI smoke-test host.

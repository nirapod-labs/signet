name: rn-example
on:
  push:
    paths:
      - "react-native/SignetApp/**"
      - "react-native/react-native-signet/**"
      - "apple/**"
      - "android/**"
      - ".github/workflows/ci-rn-example.yml"
  pull_request:
    paths:
      - "react-native/SignetApp/**"
      - "react-native/react-native-signet/**"
      - "apple/**"
      - "android/**"
      - ".github/workflows/ci-rn-example.yml"
permissions:
  contents: read
jobs:
  rn-example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build the React Native example app
        run: |
          echo "react-native example build is not yet wired"
          exit 1

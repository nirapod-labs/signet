name: ci-flutter
on:
  push:
    paths: ["flutter/**", "apple/**", "android/**", ".github/workflows/ci-flutter.yml"]
  pull_request:
    paths: ["flutter/**", "apple/**", "android/**"]
permissions:
  contents: read
jobs:
  flutter-binding:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Analyze + test the Flutter binding
        run: |
          echo "ci-flutter: the Pigeon binding is not yet implemented."
          echo "Present and red until flutter/ lands (Pigeon codegen + analyze/test + example e2e)."
          exit 1

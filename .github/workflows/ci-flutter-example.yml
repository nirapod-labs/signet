name: flutter-example
on:
  push:
    paths:
      - "flutter/SignetApp/**"
      - "flutter/signet/**"
      - "apple/**"
      - "android/**"
      - ".github/workflows/ci-flutter-example.yml"
  pull_request:
    paths:
      - "flutter/SignetApp/**"
      - "flutter/signet/**"
      - "apple/**"
      - "android/**"
      - ".github/workflows/ci-flutter-example.yml"
permissions:
  contents: read
jobs:
  flutter-example:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build the Flutter example app
        run: |
          echo "flutter example build is not yet wired"
          exit 1

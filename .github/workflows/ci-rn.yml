name: ci-rn
on:
  push:
    paths: ["react-native/**", "apple/**", "android/**", ".github/workflows/ci-rn.yml"]
  pull_request:
    paths: ["react-native/**", "apple/**", "android/**"]
permissions:
  contents: read
jobs:
  rn-binding:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Nitrogen no-drift + build the RN binding
        run: |
          echo "ci-rn: the Nitro binding is not yet implemented."
          echo "Present and red until react-native/ lands (Nitrogen no-drift + TS + example; macOS lane)."
          exit 1

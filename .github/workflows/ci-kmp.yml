name: ci-kmp
on:
  push:
    paths: ["kmp/**", "android/**", "apple/**", ".github/workflows/ci-kmp.yml"]
  pull_request:
    paths: ["kmp/**", "android/**", "apple/**"]
permissions:
  contents: read
jobs:
  kmp-binding:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build the KMP binding + Swift-consumer + conformance runners
        run: |
          echo "ci-kmp: the KMP binding is not yet implemented."
          echo "Present and red until kmp/ lands (androidMain + iosMain cinterop + Swift-consumer)."
          exit 1

name: kmp-lib
on:
  push:
    paths:
      - "kmp/signet/**"
      - "apple/**"
      - "android/**"
      - ".github/workflows/ci-kmp-lib.yml"
  pull_request:
    paths:
      - "kmp/signet/**"
      - "apple/**"
      - "android/**"
      - ".github/workflows/ci-kmp-lib.yml"
permissions:
  contents: read
jobs:
  kmp-lib:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build the KMP binding
        run: |
          echo "kotlin multiplatform build is not yet wired"
          exit 1

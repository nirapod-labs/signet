name: android-core
on:
  push:
    paths:
      - "android/**"
      - ".github/workflows/ci-android.yml"
  pull_request:
    paths:
      - "android/**"
      - ".github/workflows/ci-android.yml"
permissions:
  contents: read
jobs:
  android-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build the Android core
        run: |
          echo "android core build is not yet wired"
          exit 1

{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "lint": {},
    "format": { "cache": false },
    "build": { "dependsOn": ["^build"] },
    "test": { "dependsOn": ["build"] }
  }
}
